interface:
  display_name: "国赛全队建模协作"
  short_description: "让全队用中文读懂、建模、计算、验证和写清结论"
  default_prompt: "使用 $cumcm-modeling-lead 处理这道国赛题。先用浅显中文说明现在做到哪一步、这一步解决什么、为什么现在做、怎样算完成；让全队共同理解题意、模型、结果和局限。默认少建文件，先独立读题，再比较模型，跑真实计算并验证，最后把可信结果交给论文。"

